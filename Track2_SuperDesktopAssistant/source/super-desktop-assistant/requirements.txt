# Core
openai>=1.50.0
httpx>=0.27.0
python-dotenv>=1.0.0
ai-coin>=0.3.1    # 统一 AI 接入层（多供应商/工具循环/结构化输出）

# UI
gradio>=4.0.0
pywebview>=4.0.0

# Utilities
loguru>=0.7.0
Pillow>=10.0.0

# ── 可选：本地推理增强 ──
# STT: faster-whisper (本地语音识别)
#   pip install faster-whisper
# TTS: edge-tts (免费语音合成)
#   pip install edge-tts
# PDF: PyPDF2 (PDF 文本提取)
#   pip install PyPDF2
